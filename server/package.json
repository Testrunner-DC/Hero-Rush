{
  "name": "hero-rush-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:packages": "npm --prefix .. run build:packages",
    "dev": "npm run build:packages && tsx watch src/index.ts",
    "build": "npm run build:packages && tsc",
    "start": "node dist/index.js",
    "test": "npm run build:packages && vitest run"
  },
  "dependencies": {
    "@hero-rush/game-core": "*",
    "@hero-rush/protocol": "*",
    "@supabase/supabase-js": "^2.49.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.7.2",
    "vitest": "^4.1.9"
  }
}
